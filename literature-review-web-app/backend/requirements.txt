# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.34.0

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LLM — OpenRouter via HTTP (OpenAI-compatible)
httpx>=0.28.1

# Embeddings fallback (local, no API needed)
sentence-transformers>=3.0.0

# Academic APIs
arxiv>=2.1.3
semanticscholar>=0.8.4
scholarly>=1.7.11

# Caching — redis[asyncio] replaces aioredis for Python 3.13 compatibility
redis[asyncio]>=5.0.8

# PDF generation
reportlab>=4.2.2
fpdf2>=2.7.9

# PDF text extraction
pypdf>=4.3.1

# ML / embeddings / clustering
scikit-learn>=1.5.2
numpy>=2.0.0

# Testing
hypothesis>=6.112.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-httpx>=0.30.0
pytest-cov>=5.0.0
pytest-benchmark>=4.0.0
